# 🚀 تعليمات الرفع على الاستضافة

## الخطوات السريعة:

### 1. **التحضير قبل الرفع**
```bash
# تأكد من التبعيات
npm install --production

# اختبر السيرفر محلياً
npm start
```

### 2. **Heroku (الأسهل)**
```bash
# تثبيت Heroku CLI
npm install -g heroku

# تسجيل الدخول
heroku login

# إنشاء تطبيق
heroku create your-app-name

# رفع المشروع
git push heroku main

# عرض السجلات
heroku logs --tail
```

### 3. **Railway (ممتازة وسهلة)**
- اذهب إلى railway.app
- اربط حساب GitHub
- اختر المستودع
- ستحتاج .env ملف مع MONGODB_URI

### 🗄️ **إعداد MongoDB على Railway:**

#### الخطوة 1: إنشاء قاعدة بيانات MongoDB Atlas (مجانية)
1. اذهب إلى [mongodb.com/cloud/atlas](https://www.mongodb.com/cloud/atlas)
2. سجل حساب جديد أو سجل دخول
3. اختر "Create a Free Cluster"
4. اختر AWS وتوفر مجاني (M0)
5. انقر "Create Cluster"
6. في القائمة اليسرى اختر "Database Access"
7. أنشئ مستخدماً جديداً (username + password)
8. في "Network Access" اختر "Add IP Address" وأضف `0.0.0.0/0` (للسماح من أي مكان)
9. اضغط على "Connect" في الـ Cluster الخاص بك
10. اختر "Drivers" وانسخ connection string
11. استبدل `<password>` بـ كلمة المرور التي أنشأتها
12. استبدل `myFirstDatabase` بـ `certificates`

#### الخطوة 2: إضافة MongoDB_URI إلى Railway
1. في Railway Dashboard اختر مشروعك
2. اذهب إلى "Variables"
3. أضف متغير جديد:
   - **Key**: `MONGODB_URI`
   - **Value**: الـ connection string من MongoDB Atlas
   
مثال:
```
MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.mongodb.net/certificates
```

#### الخطوة 3: تأكد من package.json
```json
{
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mongoose": "^7.5.0",
    "dotenv": "^16.3.1"
  }
}
```

#### الخطوة 4: رفع التحديثات
```bash
git add .
git commit -m "إضافة MongoDB للمشروع"
git push
```

Railway سيعيد بناء التطبيق تلقائياً وسيتصل بـ MongoDB 🎉

### 4. **Render**
- اذهب إلى render.com
- إنشاء "Web Service"
- اربط GitHub
- Build: npm install
- Start: npm start

## 🔐 الأمان على الإنتاج:

### 1. **تغيير رمز PIN (مهم جداً)**
- عدل `login.html`
- ابحث عن: `if(pin === "1234")`
- غيّره برقم سري جديد قوي

### 2. **إضافة HTTPS**
- معظم الاستضافات توفره تلقائياً
- لا تخزن بيانات حساسة في localStorage

### 3. **النسخ الاحتياطي**
- احفظ مجلد `certificates-data/` بانتظام (البيانات القديمة إن وجدت)
- استخدم git للنسخ الاحتياطي من الكود
- ⚠️ **ملاحظة مهمة**: إذا كنت تنقل من نظام الملفات إلى MongoDB، اتبع خطوات نقل البيانات أدناه

## 📥 نقل البيانات القديمة إلى MongoDB:

إذا كنت استخدمت النسخة القديمة (بدون MongoDB)، اتبع هذه الخطوات:

```bash
# 1. تأكد من تثبيت المتطلبات الجديدة
npm install mongoose dotenv

# 2. قم بتشغيل السيرفر الجديد (سيتصل بـ MongoDB)
npm start

# 3. استخدم مجلد certificates-data القديم:
#    اختر أحد الخيارات:

## الخيار أ: إعادة حفظ الشهادات يدويًا
# افتح http://localhost:3000/admin.html وأضف الشهادات من جديد

## الخيار ب: نقل الملفات برمجياً (متقدم)
# استخدم السكريبت التالي:
```

### سكريبت نقل البيانات:
```javascript
const fs = require('fs');
const path = require('path');
const mongoose = require('mongoose');

// (انسخ Schema من server.js)
// ثم قم بتشغيل هذا السكريبت لرفع البيانات القديمة
```

## ⚠️ حذف certificates-data بعد النقل:

بعد التأكد من أن جميع البيانات نُقلت إلى MongoDB بنجاح:
```bash
rm -r certificates-data/  # في Linux/Mac
rmdir /s certificates-data  # في Windows
```


## 📋 متطلبات الاستضافة:

- ✅ Node.js 14+
- ✅ npm
- ✅ 100MB مساحة تخزين حد أدنى
- ✅ 512MB RAM (كافي لتطبيق صغير)

## 🔗 URL الاستضافة:

بعد الرفع، ستصل الروابط:
- **الرئيسية**: https://your-domain.com
- **لوحة التحكم**: https://your-domain.com/admin.html
- **موقع أولياء الأمور**: https://your-domain.com/parents.html
- **API**: https://your-domain.com/api/certificates/list

## ⚡ نصائح الأداء:

1. استخدم CDN للملفات الثابتة
2. اضغط الصور قبل الرفع
3. استخدم caching للـ API
4. راقب استخدام الذاكرة والمعالج

## 🐛 إزالة الأخطاء:

إذا واجهت مشاكل:
1. افحص سجلات الخادم (logs)
2. تأكد من أن PORT صحيح
3. تحقق من أن node_modules موجود
4. جرب npm install مجدداً

---

**تم التحضير بواسطة:** نظام إدارة الشهادات 🎓
**التاريخ:** 6 فبراير 2026
