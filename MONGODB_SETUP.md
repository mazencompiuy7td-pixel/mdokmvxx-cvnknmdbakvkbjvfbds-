# 🗄️ دليل MongoDB للمشروع

## لماذا MongoDB؟

**المشكلة السابقة:**
- البيانات حُفظت على نظام الملفات المحلي
- عند الرفع على Railway، الملفات تُحذف عند إعادة التشغيل
- النتيجة: "الشهادة غير موجودة" بعد الرفع على السحابة

**الحل باستخدام MongoDB:**
- قاعدة بيانات قوية وآمنة
- البيانات تبقى محفوظة دائماً
- تدعم البحث والفهرسة السريعة
- مجانية للاستخدام الصغير (MongoDB Atlas)

---

## الخطوات السريعة:

### 1️⃣ إعداد MongoDB Atlas (مرة واحدة فقط)

#### أ) إنشاء حساب:
```
1. اذهب إلى: https://www.mongodb.com/cloud/atlas
2. اضغط "Sign up" أو "Register"
3. استخدم بريد إلكتروني وكلمة مرور قوية
4. أكمل عملية التحقق من البريد
```

#### ب) إنشاء cluster:
```
1. بعد تسجيل الدخول، اضغط "Create a free cluster"
2. اختر:
   - Cloud Provider: AWS
   - Region: اختر الأقرب لموقعك (الشرق الأوسط: sa-east-1)
   - Cluster Tier: M0 (مجاني)
3. اضغط "Create"
4. انتظر 2-3 دقائق حتى ينتهي الـ cluster
```

#### ج) إنشاء مستخدم:
```
1. من الجانب الأيسر اختر "Database Access"
2. اضغط "Add New Database User"
3. اختر "Password" كطريقة المصادقة
4. أدخل:
   - Username: اسم المستخدم (مثل: certificates_admin)
   - Password: كلمة مرور قوية ⚠️ احفظها!
5. اضغط "Add User"
```

#### د) السماح بالاتصالات:
```
1. من الجانب الأيسر اختر "Network Access"
2. اضغط "Add IP Address"
3. اختر "Allow access from anywhere" أو أضف 0.0.0.0/0
4. اضغط "Confirm"

⚠️ تحذير: هذا يسمح من أي مكان، لكنك تحمي نفسك برمز المرور القوي
```

#### هـ) الحصول على connection string:
```
1. من الصفحة الرئيسية اضغط على cluster الذي أنشأته
2. اضغط "Connect"
3. اختر "Drivers"
4. انسخ الـ connection string
5. سيبدو هكذا:
   mongodb+srv://username:password@cluster0.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
6. استبدل:
   - username: باسم المستخدم الذي أنشأته
   - password: بكلمة المرور
   - myFirstDatabase: ب certificates
```

الـ connection string النهائي يجب أن يبدو هكذا:
```
mongodb+srv://certificates_admin:MyStrongPassword123@cluster0.mongodb.net/certificates?retryWrites=true&w=majority
```

---

### 2️⃣ إضافة المتغيرات إلى Railway

في لوحة تحكم Railway:

```
1. اختر مشروعك من الصفحة الرئيسية
2. اختر التطبيق (الـ web service)
3. اذهب إلى علامة "Variables"
4. أضف متغير جديد:
   
   KEY: MONGODB_URI
   VALUE: الـ connection string من MongoDB Atlas
```

مثال كامل:
```
MONGODB_URI=mongodb+srv://certificates_admin:MyStrongPassword123@cluster0.mongodb.net/certificates?retryWrites=true&w=majority
```

---

### 3️⃣ تحديث المشروع المحلي

```bash
# تثبيت المتطلبات الجديدة
npm install mongoose dotenv

# أو إذا كنت تستخدم yarn:
yarn add mongoose dotenv

# تحديث البيانات على GitHub
git add package.json package-lock.json server.js .env.example
git commit -m "تحويل من نظام الملفات إلى MongoDB"
git push
```

---

### 4️⃣ اختبار محلياً (اختياري)

إنشاء ملف `.env` محلي:
```
MONGODB_URI=mongodb+srv://certificates_admin:MyStrongPassword123@cluster0.mongodb.net/certificates?retryWrites=true&w=majority
PORT=3000
```

ثم:
```bash
npm start
```

ستراها في الـ console:
```
✅ تم الاتصال بـ MongoDB بنجاح
🎓 خادم الشهادات يعمل على المنفذ 3000
```

---

## ✅ التحقق من نجاح الإعداد

بعد رفع التحديثات على Railway:

1. **اذهب إلى موقعك:** `https://your-railway-domain.com`
2. **افتح المحرر:** `https://your-railway-domain.com/admin.html`
3. **أضف شهادة اختبار** بـ رقم: `9876543567890`
4. **افتح الموقع الأساسي:** `https://your-railway-domain.com/parents.html`
5. **اختبر البحث** عن نفس الرقم
6. ✅ **إذا ظهرت الشهادة، فالإعداد صحيح!**

---

## 🔧 استكشاف الأخطاء الشائعة

### ❌ "خطأ في الاتصال بـ MongoDB"

**الحل:**
- تأكد من connection string صحيح
- تأكد من أن IP Address مسموح (Network Access)
- تأكد من كلمة المرور صحيحة

### ❌ "الشهادة غير موجودة" على Railway لكن موجودة محلياً

**المشكلة:** أنت تستخدم قاعدة بيانات مختلفة محلياً وعلى Railway

**الحل:**
- استخدم نفس MONGODB_URI في المكانين
- أو أعد إضافة الشهادات على Railway

### ❌ "خطأ: ECONNREFUSED"

**المشكلة:** لا يستطيع الاتصال بـ MongoDB

**الحل:**
- تأكد من تشغيل MongoDB (إذا كنت تستخدم MongoDB محلياً)
- أو تأكد من أن MONGODB_URI صحيح

---

## 📊 مراقبة البيانات (MongoDB Atlas)

للاطلاع على البيانات المحفوظة:

```
1. اذهب إلى: https://cloud.mongodb.com
2. اختر الـ cluster
3. اضغط "Browse Collections"
4. ستجد جميع الشهادات المحفوظة ✅
```

---

## 💡 نصائح أمان إضافية

1. **غيّر كلمة المرور ON تحتاج:** 
   - انقر على username في "Database Access"
   - اختر "Edit"
   - اضغط "Change Password"

2. **احفظ كلمة المرور جيداً:**
   - استخدم مدير كلمات مرور
   - لا تشاركها مع أحد

3. **عمل نسخة احتياطية:**
   ```bash
   # MongoDB Atlas يعمل نسخ احتياطية تلقائية
   # يمكنك استدعاء البيانات من لوحة التحكم
   ```

---

## 🎉 اكتملت! الآن يمكنك:

✅ تخزين البيانات بأمان  
✅ البحث السريع عن الشهادات  
✅ حذف البيانات بسهولة  
✅ عدم فقد البيانات عند إعادة التشغيل  
✅ توسيع التطبيق مستقبلاً  

---

**الدعم:**
إذا واجهت مشكلة، راجع:
- [توثيق MongoDB Atlas](https://docs.atlas.mongodb.com/)
- [توثيق Mongoose](https://mongoosejs.com/)
- سجلات Railway (`Railway Dashboard > Logs`)

